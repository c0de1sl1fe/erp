# 📘 Microservice: HTML Gantt + Stats

Небольшой HTTP-сервис на **Python/Flask**, который по входному JSON генерирует:

- **/gantt** — HTML-фрагмент диаграммы Ганта  
- **/stats** — HTML-фрагмент со статистикой проекта  

HTML полностью автономный, содержит встроенные стили и может быть напрямую использован в **Jmix** (`HtmlComponent`, `HtmlBox`).  
Сервис также сохраняет итоговые HTML-файлы (`gantt.html`, `stats.html`) для локального просмотра.

---

# 📦 Установка и запуск

## 🔹 1. Создать и активировать виртуальное окружение (опционально)

```bash
python -m venv venv
source venv/bin/activate          # macOS / Linux
# .\venv\Scripts\activate       # Windows
```

---

## 🔹 2. Установить зависимости

```bash
pip install -r requirements.txt
```

---

## 🔹 3. Запустить сервер

```bash
python main.py
```

### 🔌 Порты по умолчанию

- http://127.0.0.1:8081/gantt  
- http://127.0.0.1:8081/stats  

---

# 📄 Формат входного JSON

Используется единый JSON для обоих эндпоинтов:

```json
{
  "id": "8fa37d20-7b19-4e94-b01a-cc48e8275aaf",
  "name": "ERP Development",
  "url": "http://localhost:8080/project/edit/8fa37d20-7b19-4e94-b01a-cc48e8275aaf",
  "startDate": "2025-01-10",
  "plannedEnd": "2025-03-01",
  "actualEnd": null,
  "theme": "dark",
  "tasks": [
    {
      "id": "d8723123-8217-4c14-9ba3-2c5b519832aa",
      "name": "Импорт legacy данных",
      "url": "http://localhost:8080/task/edit/d8723123-8217-4c14-9ba3-2c5b519832aa",
      "status": "IN_PROGRESS",
      "plannedStart": "2025-01-12",
      "plannedEnd": "2025-01-20",
      "actualEnd": "2025-01-22",
      "blocks": [
        {
          "id": "a3e0d8d3-81b1-4bb7-af57-fbb8e7c433aa",
          "name": "Подготовка структуры",
          "url": "http://localhost:8080/block/edit/a3e0d8d3-81b1-4bb7-af57-fbb8e7c433aa",
          "status": "COMPLETED",
          "plannedStart": "2025-01-12",
          "plannedEnd": "2025-01-15",
          "actualEnd": "2025-01-16"
        }
      ]
    }
  ]
}
```

---

# 🏷 Статусы

## ProjectStatus

```text
DRAFT
IN_PROGRESS
ON_HOLD
COMPLETED
```

## BlockStatus

```text
WAITING
READY
IN_PROGRESS
COMPLETED
```

Для статистики используются поля `status` у задач и блоков.

---

# 📊 Эндпоинт `/gantt`

**Метод:** `POST`  
**URL:** `http://localhost:8081/gantt`

## Функциональность

- принимает входной JSON  
- генерирует HTML-фрагмент диаграммы Ганта (`<main>...</main>`)  
- оборачивает его в шаблон `templates/gantt_template.html`  
- сохраняет как `gantt.html`  
- возвращает **только HTML-фрагмент** — для использования в Jmix  

### 🔧 Пример запроса

```bash
curl -X POST http://127.0.0.1:8081/gantt      -H "Content-Type: application/json"      -d @sample.json
```

---

# 📈 Эндпоинт `/stats`

**Метод:** `POST`  
**URL:** `http://localhost:8081/stats`

Использует тот же JSON, что и `/gantt`.

## 🔢 Логика подсчёта

### Задачи

- **В работе:** `IN_PROGRESS`  
- **Не начаты:** `DRAFT`, `ON_HOLD`, отсутствующий статус  
- **Завершены:** `COMPLETED`

### Блоки

- **В работе:** `IN_PROGRESS`  
- **Не начаты:** `WAITING`, `READY`, отсутствующий статус  
- **Завершены:** `COMPLETED`

## Функциональность

- генерирует HTML-фрагмент `<section class="stats-root">...</section>`  
- оборачивает в шаблон `gantt_template.html`  
- сохраняет как `stats.html`  
- возвращает HTML-фрагмент  

### 🔧 Пример запроса

```bash
curl -X POST http://127.0.0.1:8081/stats      -H "Content-Type: application/json"      -d @sample.json
```

---

# 🧩 Встраивание в Jmix

На стороне Jmix:

1. Выполнить REST-запрос к `/gantt` или `/stats`.
2. Передать HTML-фрагмент в компонент:

```java
htmlComponent.setHtml(htmlString);
```

---

# ⭐ Преимущества

- встроенные стили, никаких внешних CSS/JS  
- 100% совместимость с Jmix (один корневой элемент)  
- можно сохранять как полноценную HTML-страницу  
- легко редактировать шаблон без изменения Python-кода  

---

# 🧱 Зачем нужен `gantt_template.html`

`gantt_template.html` — HTML-шаблон, используемый для формирования полноценной страницы перед сохранением.

## 1. Корректное отображение HTML в браузере

Эндпоинты генерируют только фрагменты:

```html
<main>...</main>
<section>...</section>
```

Если сохранить их как есть:

- браузер не знает кодировку → могут появиться иероглифы  
- отсутствуют `<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`  
- возможно некорректное отображение содержимого  

Шаблон добавляет:

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
</head>
<body>
  {{content}}
</body>
</html>
```

Благодаря этому `gantt.html` и `stats.html` открываются корректно.

---

## 2. Разделение логики и структуры

- генерация HTML-фрагментов → `gantt_renderer.py`  
- HTTP-логика → `main.py`  
- обёртка результата → `gantt_template.html`  

### Преимущества

- можно менять внешний вид без изменения Python-кода  
- легко добавлять теги `<meta>`, дополнительные стили и JS  
- удобно для последующего экспорта/печати/PDF  

---

# ✅ Готово

Этот README.md можно класть прямо в репозиторий без изменений.
